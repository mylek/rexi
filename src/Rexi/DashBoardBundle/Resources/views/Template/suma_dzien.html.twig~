<div class="blok-suma-dnia">
	<h3>Suma z dnia:</h3>
	<div class="row">
		<div class="col-sm-2">
			<div class="form-group">
				<label>Dzień</label>
				<select id="jq-zmien-date" class="form-control m-b" >
				{% for dzien in dni %}
				    	<option 
					{% if dzien == data_wybrany %}
						selected
					{% endif %}
					value="{{ dzien }}">{{ dzien }}</option>
				{% endfor %}
				</select>
			</div>
		</div>
		<div class="col-sm-2">
			<div class="form-group">
				<label>Kalorii</label>
				<input type="text" disabled="" class="form-control" value="{{ suma.kalorii|number_format(2) }}">
			</div>
		</div>
		<div class="col-sm-2">
			<div class="form-group">
				<label>Bialka</label>
				<input type="text" disabled="" class="form-control" value="{{ suma.bialka|number_format(2) }}">
			</div>
		</div>
		<div class="col-sm-2">
			<div class="form-group">
				<label>Węgle</label>
				<input type="text" disabled="" class="form-control" value="{{ suma.wegle|number_format(2) }}">
			</div>
		</div>
		<div class="col-sm-2">
			<div class="form-group">
				<label>Tłuszcze</label>
				<input type="text" disabled="" class="form-control" value="{{ suma.tluszcze|number_format(2) }}">
			</div>
		</div>
		<div class="col-sm-2">
			<div class="form-group">
				<label>Cena</label>
				<input type="text" disabled="" class="form-control" value="{{ suma.cena|number_format(2) }}">
			</div>
		</div>
	</div>
	<div class="hr-line-dashed"></div>
</div>
