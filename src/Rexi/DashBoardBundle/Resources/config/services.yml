services:
    rexi_dashboard.twig.extension.rexi:
        class: Rexi\DashBoardBundle\Twig\Extension\RexiExtension
        arguments: ["@doctrine", "@security.context"]
        tags:
            - { name: twig.extension }
